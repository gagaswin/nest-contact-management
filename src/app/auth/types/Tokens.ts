export interface AccessToken {
  accessToken: string;
}

// export interface RefreshToken {
//   refreshToken: string;
// }

export interface Tokens {
  accessToken: string;
  refreshToken: string;
}
